TASK: Bundle a safe milestone summarizer and wire it into alex-ai init + milestone push, so every adopting project gets it automatically and it never blocks builds.

SCOPE REPO: /Users/bradygeorgen/Documents/workspace/alex-ai-universal

GOALS
- Always have a local summarizer script available in downstream projects (installed by alex-ai init).
- Summarizer is a no-op unless configured (N8N_SUMMARIZE_URL), never fails builds, and never throws uncaught errors.
- Milestone push remains non-blocking and skips summarizer cleanly when unavailable.
- Minimal intrusive changes; preserve existing indentation and formatting.

IMPLEMENTATION
1) Add summarizer template (new file)
   - Path: templates/n8n-summarize-milestone.js
   - Behavior:
     - Usage: node scripts/n8n-summarize-milestone.js --summary "<text>" --features "<semi-colon list>"
     - Env:
       - N8N_SUMMARIZE_URL (required to send; if unset → log skip and exit 0)
       - N8N_API_KEY (optional; if present use Authorization: Bearer <token>)
     - Payload (POST JSON): { summary, features, repo, branch, tag, commit, timestamp }
       - repo/branch/tag/commit derived via git if available; missing values allowed.
     - Strictly best-effort: set short timeouts; catch all errors; on failure log a one-line warning and exit 0.

2) Update init command to copy summarizer into target repos
   - File to edit: scripts/cli-init.js
   - After existing writes (.env.example/.env.local/.alex-ai/config.json), also ensure:
     - Create target path: <repo> / scripts/n8n-summarize-milestone.js (only if it does not already exist).
     - Source content from templates/n8n-summarize-milestone.js.
     - Create scripts/ directory if missing.
     - Print a tip:
       "Optional: Set N8N_SUMMARIZE_URL (and N8N_API_KEY) to enable milestone summaries."

3) Guard milestone push script
   - File to edit: scripts/milestone-push.sh
   - Keep all current behavior intact. After the RAG ingestion step, change the summarizer invocation to:
     - If file ./scripts/n8n-summarize-milestone.js exists → run:
       node scripts/n8n-summarize-milestone.js --summary "$summary" --features "$features" || true
     - Else log an info message and continue (no error). Do not fail the script either way.
   - Do not change indentation style or unrelated formatting.

4) Documentation update (small)
   - File to edit: README.md
   - In Quick Start / usage section, add a short note that alex-ai init creates scripts/n8n-summarize-milestone.js which is a safe no-op unless N8N_SUMMARIZE_URL is set, and can be used by the milestone push.

ACCEPTANCE CRITERIA
- Running: node bin/alex-ai init in a clean repo adds:
  - .env.example, .env.local, .alex-ai/config.json (existing behavior)
  - scripts/n8n-summarize-milestone.js (new; only if not already present)
- Running: bash scripts/milestone-push.sh -s "Test" -f "A;B" in a repo WITHOUT N8N_SUMMARIZE_URL set:
  - Succeeds with commit+tag push (as before), logs summarizer skip or success message, never throws or exits non-zero.
- With N8N_SUMMARIZE_URL set to a reachable endpoint (optionally N8N_API_KEY), the summarizer sends a POST with the expected JSON and still exits 0 even on remote errors.
- No uncaught exceptions; no breaking changes; non-interactive.

CONSTRAINTS
- Preserve existing indentation and formatting of all edited files.
- Minimal edits; do not reformat unrelated code.
- Summarizer must be idempotent and safe to ship in all repos.

TEST STEPS (maintainers)
- In this repo: node bin/alex-ai init
- In a temp git repo:
  - node /Users/bradygeorgen/Documents/workspace/alex-ai-universal/bin/alex-ai init
  - bash /Users/bradygeorgen/Documents/workspace/alex-ai-universal/scripts/milestone-push.sh -s "Integration" -f "One;Two"
  - Observe: no-op summarizer when N8N_SUMMARIZE_URL unset.
- Optional: export N8N_SUMMARIZE_URL="https://httpbin.org/post" and re-run to confirm POST.

DELIVERABLES
- New: templates/n8n-summarize-milestone.js
- Edited: scripts/cli-init.js to copy template into target repo scripts/
- Edited: scripts/milestone-push.sh to guard summarizer invocation
- Edited: README.md note in Quick Start
